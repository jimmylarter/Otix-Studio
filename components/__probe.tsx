/**
 * ⚠️ SCRATCH FILE — safe to delete. Created 13 Aug while checking whether Tailwind
 * emits `content` for a bare `after:` utility. It exports nothing and is imported
 * by nothing; the sandbox cannot delete files inside the mounted folder (CLAUDE.md
 * §8), so it is neutered here instead. Please remove it.
 */
export {};
